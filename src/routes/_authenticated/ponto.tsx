import { createFileRoute } from "@tanstack/react-router";
import { useMutation-placeholder } from "@tanstack/react-query";
export const Route = createFileRoute("/_authenticated/ponto")({
  component: () => null,
});